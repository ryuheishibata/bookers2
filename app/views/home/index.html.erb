    <% if flash[:notice] %>
      <div class="flash">
        <%= flash[:notice] %>
      </div>
    <% end %>
<% if @book.errors.any? %>
  <div id="error_explanation" class="alert alert-danger">
    <ul>

      <% @book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>

<div class="container">
  <div class="row">
   <div class="col-xs-3">
   <h2>User info</h2>

<div class="profile-container">
    <%= attachment_image_tag @user, :profile_image, :fill, 60, 60, class: "img-circle pull-left profile-thumb", fallback: "no_image.jpg"%>
    <table class="table">
      <tbody>
        <tr></tr>
        <tr>
          <th>name</th>
          <th><%= current_user.name %></th>
        </tr>
        <tr>
          <th>introduction</th>
          <th></th>
        </tr>
        
      </tbody>
    </table>
  <div class="row">
<p class=""><%= link_to "プロフィール編集", edit_user_path(@user), class: "pull-right" %></p>
  </div>
 


<h2>New book</h2>

<%= form_for(@book) do |f| %>
    <div class="field row">
      <label for="book_title">Title</label>
      <div calss="col-xs=12">
      <%= f.text_field :title %>
      </div>
    </div>
     <div class="field row">
    <label>Opinion</label>
    <div calss="col-xs=12">
    <%= f.text_area :body %>
    </div>
      </div>
    <div class="actions row">
    <%= f.submit 'Create Book' %>
    </div>

<% end %>
</div>
 </div>

 <div class="col-xs-9">
  <h1>Users</h1>
  <table>
    <tr>
      <th>Title</th>
      <th>Body</th>
    </tr>
    <% @books.each do |book| %>
    <tr>
      <td><%= book.title %></td>
      <td><%= book.body %></td>
      <td><%= link_to "Show", book_path(book.id) %></td>
      <td><%= link_to "Edit", edit_book_path(book.id) %></td>
      <td><%= link_to "Destroy", book_path(book.id), method: :delete, "data-confirm" => "Are you sure？" %></td>
    </tr>
    <% end %>
  </table>
 </div>
</div>


</div>


